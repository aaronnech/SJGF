package gameabstract;

//marker interface to simply mark objects we deem appropriate to render
//This prevents accidentally rendering an object we haven't written render
//code for
public interface Renderable {
}
